# 画像処理GUIアプリケーション 仕様書

## 現在の処理フロー

1. カメラでリアルタイム映像表示（ターゲットマーク付き）
2. 'q'キーで撮影
3. 撮影画像とgoogle.pngを合成（白色部分を撮影画像で置換）
4. 結果を保存

---

## 📱 GUIデザイン提案

### ウィンドウレイアウト

```
┌─────────────────────────────────────────────────────┐
│  画像処理GUIアプリ - K24032 大石大雅               │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────┐ │
│  │  📷 カメラ   │  │  🔄 画像合成  │  │ 💾 保存  │ │
│  │    撮影      │  │              │  │          │ │
│  └──────────────┘  └──────────────┘  └──────────┘ │
│                                                     │
│  ┌─────────────────────┐  ┌─────────────────────┐ │
│  │  撮影画像プレビュー   │  │  合成結果プレビュー  │ │
│  │                     │  │                     │ │
│  │   (640x480)         │  │   (640x480)         │ │
│  │                     │  │                     │ │
│  │  [画像表示エリア]    │  │  [画像表示エリア]    │ │
│  └─────────────────────┘  └─────────────────────┘ │
│                                                     │
│  📌 ステータス: 準備完了                           │
│  📝 ファイル名: (保存先ファイル名を表示)            │
└─────────────────────────────────────────────────────┘
```

---

## 機能仕様

### 1. カメラ撮影ボタン

- クリックでカメラウィンドウを起動
- 'q'キーで撮影完了
- 撮影した画像を左側のプレビューに表示
- ステータス表示: "撮影完了: camera_capture.png"

### 2. 画像合成ボタン

- 撮影済み画像がある場合のみ有効化
- クリックでgoogle.pngと合成処理を実行
- 合成結果を右側のプレビューに表示
- ステータス表示: "画像合成完了"

### 3. 保存ボタン

- 合成済み画像がある場合のみ有効化
- クリックでファイルダイアログを表示して保存先を選択
- デフォルト保存先: output_images/lecture05_01_k24032.png
- ステータス表示: "保存完了: [ファイルパス]"

### 4. プレビュー表示

- 左側: 撮影した画像
- 右側: 合成後の画像
- 画像サイズを自動調整（アスペクト比維持）

### 5. ステータス表示

- 処理の進行状況をリアルタイムで表示
- エラーメッセージも表示（カメラ接続失敗など）

---

## 🏗️ アーキテクチャ設計（モデル・ビュー・インターフェース）

### モデル (Model) - ビジネスロジック

```
my_module/K24032/
  ├── camera_model.py      # MyVideoCaptureクラスのラッパー
  └── image_processor.py   # 画像合成ロジック
```

### ビュー (View) - GUI表示

```
src/
  └── gui_view.py          # PySide6のウィンドウとウィジェット
```

### インターフェース (Controller) - モデルとビューの橋渡し

```
src/
  └── gui_controller.py    # イベントハンドラとデータバインディング
```

### メイン

```
main.py                    # GUIアプリ起動
```

---

## 🔧 インターフェース設計

### 必要なインターフェースメソッド

```python
class ImageProcessController:
    # カメラ操作
    def start_camera_capture(self) -> None
    def on_capture_completed(self, image: np.ndarray) -> None

    # 画像合成
    def process_image_composition(self) -> None
    def on_composition_completed(self, image: np.ndarray) -> None

    # 保存処理
    def save_result_image(self, filepath: str) -> None

    # ステータス更新
    def update_status(self, message: str) -> None

    # ボタン状態管理
    def update_button_states(self) -> None
```
